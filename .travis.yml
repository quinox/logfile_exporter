language: python
python:
  - 2.7
install: 
  - pip install -r requirements.txt
  - pip install coveralls
script: 
  - coverage run --parallel-mode program_example.py --testcases run-then-quit
  - coverage run --parallel-mode tests.py
after_success:
  - coverage combine
  - coveralls
